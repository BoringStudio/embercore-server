syntax = "proto3";
package base;

import "auth.proto";

message GeneralMessage {
    int64 time = 1;

    oneof Payload {
        auth.AuthRequest auth_request = 2;
        auth.AuthResponse auth_response = 3;
    }
}
